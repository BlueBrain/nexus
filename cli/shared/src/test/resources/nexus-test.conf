app {
  endpoint = "https://nexus.example.com/v1"
  token = "mytoken"
  retry {
    strategy = "once"
    initial-delay = 100 millis
    max-delay = 5 seconds
    max-retries = 1
    condition = "onServerError"
  }
}