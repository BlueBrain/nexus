Usage:
    nexus-cli config
    nexus-cli postgres
    nexus-cli influxdb

Nexus CLI

Options and flags:
    --help
        Display this help text.

Subcommands:
    config
        Read or write the tool configuration.
    postgres
        Postgres database projection.
    influxdb
        influxDB projection.