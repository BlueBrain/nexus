influx {
  endpoint = "http://localhost:8086"
  database = "influxdb"
  offset-file = /tmp/.nexus/influx.offset
  error-file = /tmp/.nexus/influx-errors.log
  offset-save-interval = 1s
  print {
    progress-interval = 100
  }
  projects {}
}
