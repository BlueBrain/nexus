plugins.search {
  # set to false to skip loading the plugin
  enabled = false
  # the priority of the plugin
  priority = 7
  # The fields configuration
  fields = null

  indexing {
    mapping = null
    settings = null
    query = null
    context = null
    resource-types = null
  }
}
