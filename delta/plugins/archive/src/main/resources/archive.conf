# archive plugin configuration
plugins.archive {
  # set to false to skip loading the plugin
  enabled = true
  # The priority of the plugin
  priority = 4
  # the archive ephemeral configuration
  ephemeral {
    ttl          = 5 hours
    max-duration = 3 seconds
  }
}