import { IDataLayerContext } from "../state-machines/data-layer/types";
export declare function sourceNodes({ parentSpan, webhookBody, store, deferNodeMutation, }: Partial<IDataLayerContext>): Promise<{
    deletedPages: string[];
    changedPages: string[];
}>;
