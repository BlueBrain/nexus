{
  "@id": "https://bluebrain.github.io/nexus/vocabulary/{{id}}",
  "@type": "RemoteDiskStorage",
  "endpoint": "{{endpoint}}",
  "folder": "{{folder}}",
  "default": false,
  "readPermission": "{{read}}",
  "writePermission": "{{write}}"
}