{
  "@type": [
    "AggregateElasticSearchView"
  ],
  "views": [
    {{#each views}}
    {
      "project": "{{project}}",
      "viewId": "{{viewId}}"
    }{{#unless @last}},{{/unless}}
    {{/each}}
  ]
}