{
  "@type": "Append",
  "acl": [
    {
      "identity": {
        "@type": "Group",
        "realm": "{{realm}}",
        "group": "{{group}}"
      },
      "permissions": [
        "{{{perms}}}"
      ]
    }
  ]
}