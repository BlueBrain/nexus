{
  "@type": "Subtract",
  "acl": [
    {
      "identity": {
        "@type": "Anonymous"
      },
      "permissions": [
        {{#each perms}}
        "{{this}}"{{#unless @last}},{{/unless}}
        {{/each}}
      ]
    }
  ]
}