{
  "@type": "Subtract",
  "acl": [
    {
      "identity": {
        "@type": "User",
        "realm": "{{realm}}",
        "subject": "{{sub}}"
      },
      "permissions": [
        "{{{perms}}}"
      ]
    }
  ]
}